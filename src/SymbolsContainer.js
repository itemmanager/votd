import styled from "styled-components";

export const SymbolsContainer = styled.div`
  display: flex;
  gap: 1em;
  flex-wrap: wrap;
  justify-content: center;
  margin: 1em;
  
  > * {
    max-width: 90px;
  }
`
